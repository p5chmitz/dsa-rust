#![allow(dead_code)]

// This module illustrates the list ADT by implementing a podium multiple
// ways. Each implementation creates and maintains a sorted list.
// The interface for each implementation defines the following operations:
//   1) New list: Constructor that creates a new list
//   2) Build an entry: Takes a name and an score to create a
//   complete individual list entry.
//   3) Insert: Takes an entry and inserts it into the proper location within
//   the sorted list, maintaining order by high score.
//   4) Remove: Removes (and returns) an entry.
//   5) Print: Formats and prints the list.
